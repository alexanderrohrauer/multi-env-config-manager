apiVersion: v1
kind: ConfigMap
metadata:
  name: test-json-config-base
  namespace: multi-env-config-manager
data:
  config.json: |
    {
      "db_host": "localhost",
      "db_port": 5432,
      "db_ssl": "false"
    }
