apiVersion: v1
kind: ConfigMap
metadata:
  name: test-postgresql-overlay
  namespace: multi-env-config-manager
  annotations:
    "mcfg.io/base": "test/test-postgresql-base"
    "mcfg.io/file-mode": "true"
data:
  pg_hba.conf: |
    local all all scram-sha-256
    hostssl all all 10.0.0.0/8 md5
