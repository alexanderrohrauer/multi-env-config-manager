apiVersion: v1
kind: Secret
metadata:
  name: test-secret-overlay
  namespace: multi-env-config-manager
  annotations:
    "mcfg.io/base": "multi-env-config-manager/test-secret-base"
    "mcfg.io/file-mode": "true"
type: Opaque
stringData:
  sensitive.xml: |
    <credentials>
      <password>overlay456</password>
    </credentials>