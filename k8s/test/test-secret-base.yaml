apiVersion: v1
kind: Secret
metadata:
  name: test-secret-base
  namespace: multi-env-config-manager
type: Opaque
stringData:
  sensitive.xml: |
    <credentials>
      <user>admin</user>
      <password>base123</password>
    </credentials>